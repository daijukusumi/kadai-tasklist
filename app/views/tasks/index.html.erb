<h1>タスクの一覧</h1>

<table border="1"
    <tr></tr><th></th><th>タスク</th><th>ステータス</th></tr>
    <% @tasks.each do |task| %>
        <tr><td><%= link_to task.id, task %></td><td><%= task.content %></td><td><%= task.status %></td></tr>
    <% end %>
</table>

<br>

<%= link_to "タスクの登録", new_task_path %>
