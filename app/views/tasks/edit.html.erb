<h1><%= @tasks.id %>タスクの編集</h1>

<%= form_for(@task) do |f| %>
    <%= f.label :content, "タスク" %>
    <%= f.text_field :content %>
    
    <%= f.submit "修正" %>
<% end %>

＜%= link_to "タスクの一覧に戻る", tasks_path %>